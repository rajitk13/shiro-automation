{
  "name": "github-push-notification",
  "description": "Send notification on GitHub push events",
  "inputs": {
    "repository": "{{env.GITHUB_REPOSITORY}}",
    "branch": "{{env.GITHUB_REF_NAME}}",
    "sha": "{{env.GITHUB_SHA}}"
  },
  "steps": [
    {
      "id": "get_changes",
      "type": "git.diff",
      "config": {
        "operation": "get_changes",
        "target": "HEAD"
      }
    },
    {
      "id": "summarize",
      "type": "ai.generate",
      "config": {
        "provider": "llama3",
        "prompt": "Summarize the changes in this push:\n\nFiles changed: {{steps.get_changes.files}}\n\nBranch: {{inputs.branch}}\n\nCommit: {{inputs.sha}}\n\nProvide a brief summary suitable for a team notification.",
        "system": "You are a helpful assistant that summarizes code changes for team notifications.",
        "temperature": 0.5,
        "max_tokens": 500
      },
      "depends_on": ["get_changes"]
    },
    {
      "id": "notify",
      "type": "slack.notify",
      "config": {
        "webhook_url": "{{env.SLACK_WEBHOOK_URL}}",
        "channel": "#deployments",
        "message": "New push to {{inputs.branch}} in {{inputs.repository}}:\n\n{{steps.summarize.content}}",
        "username": "Push Bot",
        "icon_emoji": ":rocket:"
      },
      "depends_on": ["summarize"]
    }
  ]
}
